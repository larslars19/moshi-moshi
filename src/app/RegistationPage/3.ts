import { Component, signal } from '@angular/core';

@Component({
  selector: 'app-root',
  templateUrl: './3.html'
})
export class Registation3 {
  protected readonly title = signal('moshi-moshi');
}
